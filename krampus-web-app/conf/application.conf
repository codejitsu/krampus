krampus.web-app {
  system-name = Krampus-System
  kafka {
    broker-list = "kafka:9092"
    zookeeper-host = "kafka:2181"
    topic = wikidata
    group-id = wikidata-web-app
  }
}

play.modules.enabled += "modules.ActorsModule"